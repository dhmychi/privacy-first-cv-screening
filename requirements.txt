# RUNTIME dependencies only (mirrors pyproject.toml [project.dependencies]).
# Kept as a flat file so the Docker image can install deps before copying source
# (better layer caching). Dev/test tools are in requirements-dev.txt.
# Fully-pinned transitive versions live in requirements.lock.
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4
# starlette is pulled by FastAPI; pinned to the security-patched line.
starlette==1.3.1
# PDF text + page rendering. pypdfium2 (Apache-2.0/BSD) renders pages and
# inspects image objects — a permissive-licensed replacement for PyMuPDF (AGPL).
pdfplumber==0.11.10
pypdfium2==5.12.1
pypdf==6.14.2
# OCR
pytesseract==0.3.13
pillow==12.3.0
# HTTP client for local LLM / embedding calls
httpx==0.28.1
# Arabic shaping + bidi for OCR text
arabic-reshaper==3.0.1
python-bidi==0.6.11
# Word (.docx) upload parsing
python-docx==1.2.0
