# Dev / test / quality toolchain.
# Mirrors pyproject.toml [project.optional-dependencies].dev.
# Local dev / CI:  pip install -r requirements.txt -r requirements-dev.txt
#            (or:  pip install -e ".[dev]")
pytest==9.1.1
coverage[toml]==7.15.2
ruff==0.15.22
mypy==2.3.0
fpdf2==2.8.7
